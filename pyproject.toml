[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "eeg_denoising"
version = "0.1.0"

[tool.setuptools.packages.find]
where = ["eeg_denoising"]